import React from "react";
import ReactDOM from "react-dom";

const WebBoard = ( props ) => {
        return (
        <div>WebBoard</div>
        )
    }

let element = document.getElementById("__tabpage_mount_point__")

ReactDOM.render(<WebBoard></WebBoard>, element);